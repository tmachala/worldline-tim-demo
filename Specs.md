Implement a .NET 10 console app that serves as a PoC integration of a WorldOnline TIM card payment terminal.

TIM comes with its own SDK as documented in `TIM-Dotnet-SDK`. There is a DLL that you will probably need to reference from your console app.

Feel free to test your work iteratively using a real testing terminal reachable at `192.168.0.21`.
Let me know if you need me to interact with the terminal physically, such as, insert a card, enter PIN etc.

For starts, I would like the demo app to be able to handle a payment happy flow.

Use CZK as the currency.
